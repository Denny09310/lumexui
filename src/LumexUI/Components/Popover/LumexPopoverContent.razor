@namespace LumexUI
@inherits LumexComponentBase

@using S = PopoverSlots

@if( Popover.Open )
{
    <PopoverWrapper Class="@GetStyles( "Wrapper" )">
        <div role="dialog"
             class="@GetStyles( nameof(S.Base) )"
             tabindex="-1"
             data-slot="base"
             data-open="@Popover.Open.ToAttributeValue()"
             @attributes="@Popover.AdditionalAttributes">
            <LumexComponent As="@As"
                            Class="@GetStyles( nameof( S.Content ) )"
                            Style="@RootStyle"
                            data-slot="content"
                            @attributes="@AdditionalAttributes">
                @ChildContent
            </LumexComponent>

            @if( Popover.ShowArrow )
            {
                <span class="@GetStyles( nameof(S.Arrow) )"
                      data-slot="arrow" />
            }
        </div>
    </PopoverWrapper>

    @if( !Popover.IsTooltip )
    {
        <div style="z-index: 9999; position: fixed; inset: 0; background-color: transparent;"
             data-popover-overlay="@Popover.Id"
             @onclick="@Popover.CloseAsync" />
    }
}
